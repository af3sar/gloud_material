function calculateSquare(number) {
    if (number < 0) {
        //return "Error: Negative numbers don't have real square roots."
    }
    return number * number;
}

let result1 = calculateSquare(4);
console.log(result1);

let result2 = calculateSquare(-3);
console.log(result2);

